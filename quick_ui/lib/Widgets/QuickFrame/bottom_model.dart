import 'dart:ui';

class BottomModel{
  final String? path;
  final String? title;
  final Color? unSelectedColor;
  final Color? selectedColor;
  final Offset? offset;
  BottomModel( {this.path, this.title,this.unSelectedColor,this.offset, this.selectedColor});

}